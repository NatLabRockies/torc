# Rust Quiz 4

What is the difference between `Option<T>` and `Result<T, E>`?

Answer in terms of:

1. what each one represents semantically
2. when you would choose one over the other
3. why Rust uses these instead of `null` and exceptions
