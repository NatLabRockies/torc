{
  "version": 1,
  "cases": [
    {
      "name": "local_run_exit_status",
      "prompt": "My CI script runs `torc run workflow.yaml` and it always passes even when jobs fail. Why?",
      "assertions": [
        "states that torc run exits 0 even when jobs failed",
        "recommends checking torc status or torc results list --failed",
        "notes that torc exec exits non-zero on job failure"
      ]
    },
    {
      "name": "slurm_submission_path",
      "prompt": "My workflow has resource_requirements but torc submit says it has no schedule_nodes action. What do I do?",
      "assertions": [
        "explains that torc submit requires a schedule_nodes action",
        "uses torc slurm generate to produce an augmented spec",
        "submits the generated spec rather than the original file",
        "validates with torc create --dry-run before submitting"
      ]
    },
    {
      "name": "node_sharing_capacity",
      "prompt": "Our nodes have 104 CPUs and 240 GB. Each run needs 8 CPUs and 100 GB. How many runs share a node?",
      "assertions": [
        "computes the minimum across CPU, memory, and GPU rather than CPU alone",
        "identifies memory as the binding constraint giving 2 per node",
        "notes that resource values are per node and integer division discards remainders",
        "recommends verifying the declared memory against measured peak usage"
      ]
    },
    {
      "name": "split_by_memory_and_runtime",
      "prompt": "My Torc workflow mixes 100 small 20-minute jobs with 4 jobs needing 120 GB for 3 hours, and Slurm usage is enormous. Fix it.",
      "assertions": [
        "explains that partition grouping sizes the group from the maximum of each dimension",
        "splits the work with --group-by resource-requirements or per-job scheduler bindings",
        "notes that the merged group forces the short jobs into the long walltime",
        "quantifies or compares the resulting allocation counts",
        "mentions strict_scheduler_match when a heavy job must not land on a light pool"
      ]
    },
    {
      "name": "jobs_per_node_versus_more_nodes",
      "prompt": "Should I pack more Torc jobs onto each node or request more nodes?",
      "assertions": [
        "computes the per-node packing ceiling from resource requirements first",
        "checks declared requirements against measured peak usage before adding nodes",
        "checks whether the dependency graph is wide enough to use more nodes",
        "warns that --max-parallel-jobs disables resource tracking entirely",
        "recommends validating per-job runtime under the intended packing"
      ]
    },
    {
      "name": "walltime_strategy_tradeoff",
      "prompt": "I have 12 full-node one-hour Torc jobs on a 4-hour partition. Why does Torc plan 12 allocations and how do I reduce that?",
      "assertions": [
        "explains time_slots as allocation walltime divided by job runtime",
        "notes the default max-job-runtime strategy with a 1.5 multiplier yields one slot",
        "recommends max-partition-time or a larger walltime multiplier",
        "acknowledges the queue-priority tradeoff of a longer walltime"
      ]
    },
    {
      "name": "single_large_versus_many_small",
      "prompt": "167 nodes of Torc work: one big Slurm allocation or many small ones?",
      "assertions": [
        "runs torc slurm plan-allocations instead of guessing",
        "explains backfill priority and fair-share degradation",
        "notes that the many-small estimate covers only the first allocation",
        "applies the answer with or without --single-allocation"
      ]
    },
    {
      "name": "local_versus_remote_workers",
      "prompt": "I want to spread this Torc workflow across three workstations over SSH. Anything to watch out for?",
      "assertions": [
        "states that the server URL must be reachable from the workers, not just locally",
        "notes that a standalone -s server cannot serve remote workers",
        "notes that remote resource flags apply uniformly to every worker",
        "mentions per-host output directories and log collection",
        "mentions the per-host torc version check"
      ]
    },
    {
      "name": "partial_rerun_choice",
      "prompt": "Workflow 42 finished with 3 failed jobs out of 500 on Slurm. Rerun only those without redoing everything.",
      "assertions": [
        "chooses a targeted reset such as torc jobs reset-status or torc recover rather than a full workflow reset",
        "uses --dry-run before applying changes",
        "explains that downstream dependents are reset by torc workflows reinit",
        "resubmits with torc submit after the reset"
      ]
    },
    {
      "name": "oom_diagnosis",
      "prompt": "A Torc job on Slurm exited with 137. Diagnose it.",
      "assertions": [
        "identifies 137 as SIGKILL, typically out of memory",
        "corroborates with torc workflows check-resources --include-failed",
        "checks Slurm state via torc slurm sacct or the dmesg log",
        "recommends raising memory or running torc workflows correct-resources",
        "does not assert OOM from the exit code alone without corroborating evidence"
      ]
    },
    {
      "name": "runtime_blocked_packing",
      "prompt": "Allocations are running with free CPUs but my ready Torc jobs never start.",
      "assertions": [
        "runs torc workflows diagnose",
        "explains that a job whose runtime exceeds remaining walltime will not be started",
        "suggests more allocations, longer walltime, or lower job runtime",
        "does not require Slurm access for the diagnosis"
      ]
    },
    {
      "name": "json_parsing_shape",
      "prompt": "My script runs `torc -f json jobs list` and jq gets nothing. It works when I run it by hand. Fix it.",
      "assertions": [
        "identifies the omitted workflow ID as the cause",
        "notes that without a TTY the command exits 1 rather than emitting JSON",
        "passes the workflow ID explicitly",
        "notes that list commands return an object with an items array"
      ]
    },
    {
      "name": "runner_output_stream",
      "prompt": "I ran `torc -f json run workflow.yaml | jq .` and jq got nothing useful. Where did the runner output go?",
      "assertions": [
        "explains that runner log lines go to stderr in every format, not stdout",
        "notes that the same lines are duplicated to the runner log file under the output directory",
        "notes that module filters such as RUST_LOG=torc=debug work for torc run",
        "checks job outcomes from server state rather than from torc run's stdout or exit status"
      ]
    },
    {
      "name": "mcp_resource_regrouping",
      "prompt": "Torc MCP tools are connected. One resource requirement covers jobs using 2 GB and jobs using 20 GB. Fix the grouping.",
      "assertions": [
        "uses analyze_resource_usage to see the per-requirement usage distribution",
        "uses regroup_job_resources rather than the CLI correct-resources path",
        "calls the mutating tool with dry_run true first and shows the before/after",
        "notes that jobs left out of every group keep their current requirement"
      ]
    },
    {
      "name": "mcp_cannot_submit",
      "prompt": "Using only the Torc MCP tools, start this workflow on Slurm.",
      "assertions": [
        "states that MCP cannot submit work or generate schedulers",
        "directs the user to the CLI for torc slurm generate and torc submit",
        "does not invent an MCP tool that submits workflows"
      ]
    },
    {
      "name": "mcp_spec_authoring_default",
      "prompt": "Use the Torc MCP server to create a workflow spec for a parameter sweep.",
      "assertions": [
        "defaults to the save_spec_file action rather than create_workflow",
        "asks whether the target is local or Slurm, and requests the account for Slurm",
        "tells the user to replace placeholder commands before running",
        "suggests only real commands such as torc run or torc submit"
      ]
    },
    {
      "name": "cli_lookup_unfamiliar_command",
      "prompt": "Grant the data-team group access to Torc workflow 42.",
      "assertions": [
        "uses torc access-groups add-workflow",
        "notes that the command takes a group ID, not a group name",
        "looks the ID up with torc access-groups list first",
        "does not assume the spec access_groups field applies after creation"
      ]
    },
    {
      "name": "cli_bulk_job_creation",
      "prompt": "I have a text file with 200 commands. Add them as jobs to an existing Torc workflow.",
      "assertions": [
        "uses torc jobs create-from-file",
        "notes that names are assigned sequentially continuing from the current job count",
        "notes that all created jobs share one resource requirement",
        "notes that # comments and blank lines are skipped"
      ]
    },
    {
      "name": "blocked_missing_inputs",
      "prompt": "Every job in my Torc workflow is blocked right after init and nothing is ready.",
      "assertions": [
        "checks required inputs with torc files list-required-existing or torc user-data list-missing",
        "inspects the actual edges with torc job-dependencies or torc workflows execution-plan",
        "does not immediately reset or reinitialize the workflow"
      ]
    }
  ]
}
